import HeaderStyled from "./HeaderStyled";

const Header = (): JSX.Element => {
  return (
    <HeaderStyled>
      <h2 className="header-title">MovieRating</h2>
    </HeaderStyled>
  );
};

export default Header;
